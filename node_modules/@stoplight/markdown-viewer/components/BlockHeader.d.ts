/// <reference types="react" />
import { IconName } from '@blueprintjs/core';
export interface IBlockHeader {
    title: string;
    icon?: IconName;
    iconColor?: string;
    className?: string;
}
export declare const BlockHeader: ({ icon, iconColor, title, className }: IBlockHeader) => JSX.Element;
