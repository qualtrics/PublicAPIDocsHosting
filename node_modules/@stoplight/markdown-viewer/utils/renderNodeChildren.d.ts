import { Node, Parent } from '@stoplight/markdown/ast-types/unist';
import * as React from 'react';
import { IComponentMapping } from '../types';
export declare const renderNodeChildren: (node: Parent | Node, components?: IComponentMapping, parentPath?: React.ReactText[]) => React.ReactNode[] | null;
