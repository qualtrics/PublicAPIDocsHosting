"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const lodash_1 = require("lodash");
const guards_1 = require("../guards");
function translateToServers(servers) {
    return servers.map(server => ({
        description: server.description,
        url: server.url,
        variables: server.variables && translateServerVariables(server.variables),
    }));
}
exports.translateToServers = translateToServers;
function translateServerVariables(variables) {
    return lodash_1.mapValues(lodash_1.pickBy(variables, guards_1.isServerVariableObject), value => ({
        default: String(value.default),
        description: value.description && String(value.description),
        enum: value.enum && lodash_1.map(value.enum, String),
    }));
}
exports.translateServerVariables = translateServerVariables;
//# sourceMappingURL=servers.js.map