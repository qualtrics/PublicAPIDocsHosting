"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const securityScheme_1 = require("./transformers/securityScheme");
const util_1 = require("./util");
const DEFAULT_VERSION = '1.0.0';
exports.transformPostmanCollectionService = document => {
    const collection = util_1.resolveCollection(document);
    return {
        id: collection.id,
        name: collection.name,
        version: collection.version ? util_1.resolveVersion(collection.version) : DEFAULT_VERSION,
        description: collection.description && util_1.transformDescriptionDefinition(collection.description),
        securitySchemes: securityScheme_1.transformSecuritySchemes(collection)
            .filter(securityScheme_1.isStandardSecurityScheme)
            .map(pss => pss.securityScheme),
    };
};
//# sourceMappingURL=service.js.map