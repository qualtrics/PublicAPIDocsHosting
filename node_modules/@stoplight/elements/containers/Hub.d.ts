import * as React from 'react';
import { ITableOfContents } from '../components/TableOfContents';
import { IProjectNode } from '../types';
import { IPageContainer } from './Page';
export interface IHub {
    srn: string;
    tabs: IPageContainer['tabs'];
    group?: string;
    className?: string;
    padding?: string;
    NotFoundComponent?: React.FC<{
        srn: string;
        error?: {
            message: string;
        };
        items: IProjectNode[];
    }>;
    pageProps?: Partial<IPageContainer>;
    tableOfContentsProps?: Omit<ITableOfContents, 'items' | 'contents'>;
}
export declare const Hub: React.FC<IHub>;
