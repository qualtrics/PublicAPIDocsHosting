import * as React from 'react';
import { IPage } from '../components/Page';
export interface IPageContainer {
    srn: string;
    tabs: IPage['tabs'];
    group?: string;
    version?: string;
    actions?: IPage['actions'];
    padding?: string;
    className?: string;
    shadows?: boolean;
    scrollInnerContainer?: boolean;
}
export declare const Page: React.FC<IPageContainer>;
