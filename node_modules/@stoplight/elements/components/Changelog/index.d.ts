import * as React from 'react';
import { IChange } from '../../types';
export interface IChangelogProps {
    padding?: string;
    className?: string;
    changes?: IChange[];
}
export declare const Changelog: React.NamedExoticComponent<IChangelogProps>;
