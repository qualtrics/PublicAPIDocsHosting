import * as React from 'react';
import { IDiagnostic } from '../../types';
export interface IModelProps {
    value: any;
    errors?: IDiagnostic[];
    className?: string;
    title?: string;
    maxRows?: number;
    actions?: React.ReactElement;
}
export declare function Model({ value, className, title, maxRows, actions, errors }: IModelProps): JSX.Element;
