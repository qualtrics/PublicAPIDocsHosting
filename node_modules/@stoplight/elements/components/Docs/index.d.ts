import { IRoot } from '@stoplight/markdown';
import * as React from 'react';
import { INodeInfo } from '../../types';
export interface IDocs {
    node: INodeInfo;
    padding?: string;
}
export declare const Docs: ({ node, padding }: IDocs) => JSX.Element;
export declare const DocsHeadings: ({ tree, padding, containerRef, }: {
    tree: IRoot;
    padding: string;
    containerRef: React.MutableRefObject<HTMLDivElement | null>;
}) => JSX.Element;
