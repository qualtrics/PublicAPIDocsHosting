import * as React from 'react';
import { SuggestionFunc } from '../../hooks/useSuggestion';
import { RequestMakerStore } from '../../stores/request-maker';
declare type SuggestionBarProps = {
    suggestions: Array<SuggestionFunc<RequestMakerStore>>;
};
export declare const SuggestionBar: React.FC<SuggestionBarProps>;
export {};
