import { IHttpContent } from '@stoplight/types';
import * as React from 'react';
export interface ISchema {
    className?: string;
    value?: IHttpContent['schema'];
    examples?: IHttpContent['examples'];
}
export declare const Schema: React.FunctionComponent<ISchema>;
