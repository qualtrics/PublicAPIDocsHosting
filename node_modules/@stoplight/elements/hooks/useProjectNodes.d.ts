import { IProjectNode } from '../types';
export declare function useProjectNodes(srn: string, opts?: {
    group?: string;
}): {
    data: {
        items: IProjectNode[];
    } | {
        items: IProjectNode[];
        pageInfo: {
            hasNextPage: boolean;
            hasPreviousPage: boolean;
            startCursor: string;
            endCursor: string;
        };
        totalCount: number;
    };
    isLoading: boolean;
    error: Error | undefined;
};
