"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const tslib_1 = require("tslib");
const swr_1 = tslib_1.__importDefault(require("swr"));
const useFetchClient_1 = require("../utils/useFetchClient");
function useNodeRaw(srn, opts) {
    const fetch = useFetchClient_1.useFetchClient();
    let query = `?srn=${srn}`;
    if (opts) {
        if (opts.deref) {
            query = `${query}&deref=${opts.deref}`;
        }
    }
    return swr_1.default(`/nodes.raw${query}`, (input, init) => fetch(input, init).then(res => {
        if (!res.ok) {
            throw new Error(`${res.status} ${res.statusText}`);
        }
        return res.text();
    }));
}
exports.useNodeRaw = useNodeRaw;
//# sourceMappingURL=useNodesRaw.js.map