import * as React from 'react';
import { RequestMakerStore } from '../stores/request-maker';
export declare type SuggestionFunc<T> = (input: T) => React.ReactNode;
export declare const useSuggestion: <T>(possibleSuggestions: readonly SuggestionFunc<T>[], input: T) => React.ReactNode;
export declare const createContentTypeSuggestion: (condition: (store: RequestMakerStore) => boolean, contentTypeHeaderValue: string, render: (performResolution: () => void) => React.ReactNode) => SuggestionFunc<RequestMakerStore>;
