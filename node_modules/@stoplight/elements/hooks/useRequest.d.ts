import { Dictionary, Primitive } from '@stoplight/types';
export declare type UseRequestState<T> = {
    isLoading: boolean;
    data?: T;
    error?: Error;
};
interface IRequestConfig {
    pathname: string;
    params: Dictionary<Primitive, string>;
}
export declare function useRequest<T>(args: IRequestConfig): UseRequestState<T>;
export {};
