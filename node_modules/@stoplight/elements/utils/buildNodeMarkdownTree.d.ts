import { IRoot } from '@stoplight/markdown';
import { NodeType } from '@stoplight/types';
import { IDiagnostic } from '../types';
export declare function buildNodeMarkdownTree(type: NodeType, data: unknown, errors?: IDiagnostic[]): IRoot;
