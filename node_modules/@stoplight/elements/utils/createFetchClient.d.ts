import { Dictionary } from '@stoplight/types';
export interface IFetchProps {
    host: string;
    headers: Dictionary<string, string> | null;
}
export declare const createFetchClient: (opts: IFetchProps) => typeof fetch;
