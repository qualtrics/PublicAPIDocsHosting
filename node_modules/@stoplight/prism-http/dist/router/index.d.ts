import { IPrismComponents } from '@stoplight/prism-core';
import { IHttpOperation } from '@stoplight/types';
import { IHttpConfig, IHttpRequest } from '../types';
declare const route: IPrismComponents<IHttpOperation, IHttpRequest, unknown, IHttpConfig>['route'];
export default route;
