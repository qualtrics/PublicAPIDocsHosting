import { Command } from '@oclif/command';
export interface IResponses {
    name: string;
    description: string;
    author: string;
    repository: string;
    license: 'apache2' | false;
    react: boolean;
    ci: 'circle';
    quality: 'codeclimate';
}
export default class CreateLibCommand extends Command {
    static description: string;
    static examples: string[];
    static args: never[];
    static flags: {};
    run(): Promise<void>;
    writeScaffold(targetDir: string): void;
    writeIgnore(targetDir: string): void;
    writeLicense(targetDir: string, responses: IResponses): void;
    writeStorybook(targetDir: string, responses: IResponses): void;
    writeCi(targetDir: string, responses: IResponses): void;
    writeQuality(targetDir: string, responses: IResponses): void;
    writePackage(targetDir: string, responses: IResponses): void;
    createPackage(responses: IResponses): any;
    writeReadme(targetDir: string, responses: IResponses): void;
    writeSrc(targetDir: string, responses: IResponses): void;
    templateDir: () => string;
}
