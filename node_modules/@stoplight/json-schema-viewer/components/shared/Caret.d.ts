import { IIconProps } from '@stoplight/ui-kit';
import * as React from 'react';
export interface ICaret {
    isExpanded: boolean;
    style?: React.CSSProperties;
    size?: IIconProps['iconSize'];
}
export declare const Caret: React.FunctionComponent<ICaret>;
