import * as Unist from 'unist';
import { IRoot } from './ast-types/mdast';
import { Reader } from './reader';
export declare class Builder {
    reader: Reader;
    root: IRoot;
    constructor(reader?: Reader);
    addMarkdown(markdown: string): this;
    addChild(node: Unist.Node): this;
    toString(): string;
}
